OPTION(MODE_SERVICE "Build as a service / linux daemon" OFF)
OPTION(TRACE_ENABLE "Enable trace" OFF)
OPTION(LOG_ENABLE "Enable log" OFF)
SET(COMPILE_DEFINITIONS "")
if (MODE_SERVICE)
  LIST(APPEND COMPILE_DEFINITIONS MODE_SERVICE)
else()
  LIST(APPEND COMPILE_DEFINITIONS MODE_STANDALONE)
endif()
if (TRACE_ENABLE)
  LIST(APPEND COMPILE_DEFINITIONS TRACE_ENABLE)
endif()
if (LOG_ENABLE)
  LIST(APPEND COMPILE_DEFINITIONS LOG_ENABLE)
endif()
